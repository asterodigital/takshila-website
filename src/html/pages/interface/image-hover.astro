---
import AdminLayout from '../../layouts/admin/admin-layout.astro'
import ComponentPreview from '../../components/component-preview/component-preview.astro'
import Toc from '../../components/toc/toc.astro'
import PageHeader from '../../components/common/page-header.astro'
import { getAssetPrefix, getPathPrefix } from '../../../utils/path.js'

// Page header configuration with navigation breadcrumbs
const pageHeaderConfig = {
  title: 'Image Hover',
  breadcrumbs: [
    { label: 'Dashboard', href: getPathPrefix('/dashboard') },
    { label: 'Interface', href: getPathPrefix('/interface') },
    { label: 'Image Hover', class: 'active' }
  ]
}

// Image hover examples
const code_1 = `<figure class="img-lift pt-3">
  <img src="${getAssetPrefix('/assets/img/placeholder-images/placeholder-image-300x200.png')}" class="img-fluid rounded-3" alt="image">
</figure>
`

const code_2 = `<figure class="img-scale rounded-3">
  <img src="${getAssetPrefix('/assets/img/placeholder-images/placeholder-image-300x200.png')}" class="img-fluid" alt="image">
</figure>
`

const code_3 = `<figure class="img-grayscale">
  <img src="${getAssetPrefix('/assets/img/placeholder-images/placeholder-image-colored-300x200.png')}" class="img-fluid rounded-3" alt="image">
</figure>
`

// Shared preview configuration for consistent component display
const previewConfig = {
  cssCode: '',
  jsCode: ''
}
---

<AdminLayout
  title="Image Hover"
  description="Image hover examples"
  currentPath="interface/image-hover"
  enableComponentPreview={true}
>
  <PageHeader {...pageHeaderConfig} />
  <div class="row">
    <div class="col-lg-9 col-xl-9 order-1 order-lg-0">
      <div class="row">
        <div class="col-12">
          <p class="my-0">
            Image hover effects are visual changes or transformations applied to an image when a
            user hovers over it with their mouse. The goal of image hover effects is to enhance the
            user experience by providing interactive feedback, making the design more engaging and
            dynamic.
          </p>
        </div>
      </div>
      <div class="row js-toc-content">
        <div class="col-12">
          <div class="my-3">
            <h3 class="mb-0" id="image-lift">Image lift</h3>
            <p class="py-2">
              Image lift is a design effect where an image component appears to "lift" or elevate on
              hover
            </p>

            <ComponentPreview
              title="Image Lift"
              height="310px"
              component={code_1}
              htmlCode={code_1}
              {...previewConfig}
            />
          </div>
          <div class="my-3">
            <h3 class="mb-0" id="image-scale">Image scale</h3>
            <p class="py-2">
              Image scale is a visual effect where an image grows in size on hover, creating the
              illusion of zooming in while maintaining its position within the container.
            </p>

            <ComponentPreview
              title="Image Scale"
              height="290px"
              component={code_2}
              htmlCode={code_2}
              {...previewConfig}
            />
          </div>
          <div class="my-3">
            <h3 class="mb-0" id="image-grayscale">Image grayscale</h3>
            <p class="py-2">
              This hover effect transforms an image from full color to black and white, and reverts
              back to color when hovered, creating an interactive visual transition.
            </p>

            <ComponentPreview
              title="Image Grayscale"
              height="290px"
              component={code_3}
              htmlCode={code_3}
              {...previewConfig}
            />
          </div>
        </div>
      </div>
    </div>
    <Toc />
  </div>
</AdminLayout>
